import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.biblegamesproject.exodussurvivors",
  appName: "Exodus Survivors",
  webDir: ".output/public",
};

export default config;
